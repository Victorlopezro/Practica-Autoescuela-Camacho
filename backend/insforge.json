{
  "name": "autoescuela-camacho-api",
  "type": "node",
  "buildCommand": "pnpm install --frozen-lockfile && pnpm prisma generate && pnpm build",
  "startCommand": "node dist/src/main.js",
  "healthCheckPath": "/v1/health",
  "env": {
    "NODE_ENV": "production",
    "PORT": "3000"
  }
}
